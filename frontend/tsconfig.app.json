{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    // App build shouldn’t pull in vitest or node test globals
    "types": []
  },
  "include": [
    "src/**/*.ts",
    "src/**/*.tsx",
    "vite-env.d.ts"
  ],
  "exclude": [
    "**/*.test.ts",
    "**/*.test.tsx",
    "**/__tests__/**",
    "tests/**",
    "playwright.config.*",
    "vite.config.*",
    "vitest.setup.*",
    "src/__tests__/**"
  ]
}
